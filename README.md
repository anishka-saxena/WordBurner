# WordBurner

A FastAPI application.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
uvicorn main:app --reload
```

## Endpoints

- `GET /` — Health check
- `GET /roast` — Get a roast
